{
  "name": "wtl-otp-server",
  "version": "1.0.0",
  "description": "OTP send/verify webhook server for We The Leaders Foundation Vapi voice agent",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  }
}
